#!/usr/bin/env bash
#
# The `e` command acts as a quick shortcut to your editor.
#
# USAGE:
#
#   $ e
#   # => opens the current directory in your editor
#
#   $ e .
#   $ e /usr/local
#   # => opens the specified directory in your editor
exec "$EDITOR" "${1:-.}"