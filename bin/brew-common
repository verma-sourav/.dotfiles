#!/usr/bin/env bash
# --------------------------------------------------------------------------------------------------
# brew-common
# A script to install some commonly used homebrew packages
# --------------------------------------------------------------------------------------------------

brew install curl
brew install wget
brew install coreutils
brew install git
brew install go
brew install python3
brew install neovim

brew cleanup
